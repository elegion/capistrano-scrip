<% if method_listing.size > 0 %>
  <% groups(method_listing) do |list, name| %><% next if name != 'Class Method Summary' %>
    <h2>
      <%= 'Tasks summary' %>
      <small>(<a href="#" class="summary_toggle">collapse</a>)</small>
    </h2>

    <ul class="summary">
      <% list.each do |meth| %>
        <%= yieldall :item => meth %>
      <% end %>
    </ul>
  <% end %>
<% end %>
